{
  "name": "chw-companion-web",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "WebGPU browser fallback for CHW Companion (CHW visit triage demo).",
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build",
    "preview": "vite preview --port 4173",
    "deploy": "wrangler pages deploy dist --project-name chwcompanion"
  },
  "dependencies": {
    "@huggingface/transformers": "^3.4.0",
    "react": "^19.0.0",
    "react-dom": "^19.0.0"
  },
  "devDependencies": {
    "@types/react": "^19.0.0",
    "@types/react-dom": "^19.0.0",
    "@vitejs/plugin-react": "^4.3.0",
    "typescript": "~5.9.2",
    "vite": "^5.4.0",
    "wrangler": "^3.80.0"
  }
}
